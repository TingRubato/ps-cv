{%- comment -%}
  quadfigure.liquid: Displays a quad-splitting canvas for a dynamic image rendering.
{%- endcomment -%}

<div class="quad-figure-container">
  <canvas
    id="quadCanvas"
    width="512"
    height="512"
    class="{{ class }}"
    style="max-width: 100%;"
    aria-label="{{ alt }}"
    role="img"
  >
    Sorry, your browser doesn’t support the HTML5 <code>canvas</code> element.
  </canvas>
</div>

<script src="{{ '/assets/js/chart/quad-splitting.js' | relative_url }}" defer></script>
<script>
  // Use cache-busting if enabled
  const imagePath = "{{ 'assets/img/photo.png' | relative_url}}{% if cache_bust %}?{{ site.time | date: '%s' }}{% endif %}";
  // Initialize the quad-splitting animation
  initQuadSplitting(imagePath, '#quad-canvas');
</script>
