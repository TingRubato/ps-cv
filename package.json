{
  "scripts": {
    "format": "prettier --write .",
    "purgecss": "purgecss --config purgecss.config.js",
    "build:css": "bundle exec jekyll build && npm run purgecss",
    "postbuild": "npm run purgecss"
  },
  "devDependencies": {
    "@shopify/prettier-plugin-liquid": "1.10.2",
    "prettier": "3.8.1",
    "purgecss": "^6.0.0"
  }
}
